import { type FC } from "react"
import "./index.scss"

const TimeTracker: FC = () => {
    return (
        <input
            className="time-tracker"
            type="number"
        />
    )
}

export default TimeTracker
